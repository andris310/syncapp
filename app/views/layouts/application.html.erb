<!DOCTYPE html>
<html>
<head>
  <title>Syncapp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Raleway:400,100' rel='stylesheet' type='text/css'>
</head>
<body>
	<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="/">SyncApp</a>
    <ul class="nav">
      <li><a href="/">Home</a></li>
      <li><a href="/products">Products</a></li>
      <li><a href="/setup"></a>Set Up</li>
    </ul>
    <div class="pull-right" id="sign-in-out">
    	<% if user_signed_in? %>
    		<%= current_user.first_name %>
    		  <span class="button2"><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %>
    		<% else %>
    		<%= link_to 'Sign In', new_user_session_path %></span>
    		<% end %>
    </div>
  </div>
</div>

<%= yield %>

</body>
</html>
